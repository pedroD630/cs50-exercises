#include <cs50.h>
#include <stdio.h>

int main(void)
{
    //prompt user for a card number
    //calculus
    //take one digit yes the other not start in second-to-last
    //multiply each of the selected by 2
    //add the result of each one togheter ( add digits like 13 = 1+3+...)

    //2.


    //if is valid
    //print if is a card of VISA, AMEX or MASTERCARD
    //else
    // print INVALID
}
